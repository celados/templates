# Shared Agent Context

This directory owns the Agent Native context that will be materialized into all
Clonesite templates. It remains intentionally minimal until the shared contract,
skills, generated artifacts, and acceptance gates are designed together.
