import type { SiteConfig } from '@/site/site-config'

export const site = {
	id: 'alpha',
	name: 'Alpha',
	tagline: 'One product core, an energetic public surface.',
	description:
		'Alpha owns its routes and deployment while sharing auth, billing, data, and UI with Beta.',
} satisfies SiteConfig
