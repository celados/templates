import type { SiteConfig } from '@/site/site-config'

export const site = {
	id: 'beta',
	name: 'Beta',
	tagline: 'The same product core with an independent route tree.',
	description:
		'Beta can develop and deploy alongside Alpha without sharing generated files or build output.',
} satisfies SiteConfig
